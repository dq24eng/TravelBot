// node_modules/@types/node-nlp/index.d.ts
declare module 'node-nlp' {
    export class NlpManager {
        constructor(settings: { languages: string[], forceNER?: boolean, caseSensitive?: boolean });
        addDocument(language: string, text: string, intent: string): void;
        addNamedEntityText(entityName: string, text: string, languages: string[], examples: string[]): void;
        addAnswer(language: string, intent: string, answers: string[]): void;
        train(): Promise<void>;
        save(filename: string): Promise<void>;
        load(filename: string): Promise<void>;
        process(language: string, text: string): Promise<{ intent: string, entities: any[] }>;
        getAnswersByIntent(intent: string): string[];

        // ... Añade aquí otros métodos y propiedades que uses
    }

    // Si usas otras clases o interfaces de node-nlp, decláralas aquí también
    // Ejemplo:
    // export interface Entity {
    //     entity: string;
    //     // ... otras propiedades de la entidad
    // }

    // Si necesitas declarar tipos para valores exportados, puedes usar:
    // export const algo: string;

    // Si necesitas declarar funciones, puedes usar:
    // export function otraFuncion(parametro: tipo): tipoRetorno;
}